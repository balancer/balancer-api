

describe('CoingeckoTokenPriceFetcher', () => {
  it('Should initialize correctly', () => {

  });

  it('Should send 2 tokens off after the debounce period', () => {

  });

  it('Should send batchSize tokens off immediately', () => {

  });

  it('Should be able to handle multiple networks', () => {

  });
})